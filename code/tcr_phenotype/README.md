## Code used to create the TCR phenotype matrices in `./data/tcr_phenotype/*.`
- .sh file names match the name of the phenotype output files, described in `./data/tcr_phenotype/README.md`
- `./cdr3_aa_covariance.R` computes Shannon entropy and mutual information for CDR3 sequences
